version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Installing dependencies"
        - pip install -r requirements.txt
    build:
      commands:
        - echo "Starting Flask application"
        - python app.py
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
